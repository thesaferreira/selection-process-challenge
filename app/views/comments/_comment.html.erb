<div class="comment">
    <section class="comment-body">
        <div>
            <strong><%= comment.user.blank? ? 'Anônimo' : comment.user.name %></strong>
            <p>
                <%= comment.body %> <br>
                <em><%= time_ago_in_words(comment.created_at) %> atrás</em>
            </p>   
        </div>
    </section>
</div>